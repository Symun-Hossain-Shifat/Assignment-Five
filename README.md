1️⃣ What is the difference between var, let, and const?
var is the old variable in js old version . It called block scope variable . On the other hand , let & const are the variable in js ESC-6 . let is changeable & const is unchangeable variable.

2️⃣ What is the spread operator (...)?
spread is a important function in js ESC-6 . spread means for expand the element of an array  & object in Js.

3️⃣ What is the difference between map(), filter(), and forEach()?
map() used for do operation in  the element of an array  & return a new array, filter() used for find the elements of an array depends on condition , forEach() used for lopping the element of array and it dosent return anything by deafult.

4️⃣ What is an arrow function?
arrow function is a useful short form funcion in the js ESC-6 VERSION . It is very useful for clean the code.

5️⃣ What are template literals?
Template literals is a modern way  of call string into the bactikcs
